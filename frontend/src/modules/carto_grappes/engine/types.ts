// Re-export all types from the main types file to avoid duplication
export type {
  Village, Menage, GpsEntry, LotKey, LotMode, StatusValue,
  StatusOption, LotEntry, MenageEntry,
  EntrepreneurData, EntrepreneurGroup, LotConfig, EntrepreneurConfig,
  GrappeSummary, RegionSummary, TabKey, PlanningSubTab,
  FicheFieldDef, FicheDef, FicheEntry, HistoryEntry,
  PlanningParams, GanttItem, PlanningAlert, PlanningResult,
  RegionPlanning, PhaseDetail, FormationSession, PlanningSynthese,
  Prestataire,
} from '../types';
