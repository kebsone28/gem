name: gem_saas

services:
  # ============ NGINX REVERSE PROXY ============
  nginx:
    image: nginx:alpine
    container_name: gem-saas-proxy
    ports:
      # Ports hote dedies GEM SAAS pour eviter les collisions avec les autres apps Docker.
      - "${GEM_HTTP_PORT:-8088}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certs:/etc/nginx/certs:ro
      - ./frontend/dist:/usr/share/nginx/html:ro
    depends_on:
      - app
    networks:
      - gem_saas_internal
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1/health" ]
      interval: 30s
      timeout: 10s
      retries: 3

  # ============ NODE.JS APPLICATION ============
  app:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: gem-saas-api
    ports:
      - "${GEM_API_PORT:-5009}:3001"
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: 3001
      DATABASE_URL: postgresql://${DB_USER:-proquelec}:${DB_PASSWORD:-securepassword}@postgres:5432/${DB_NAME:-proquelec_saa}?schema=public
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_ENABLED: "true"
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
      REFRESH_TOKEN_SECRET: ${REFRESH_TOKEN_SECRET:-change-refresh-secret}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:8088,http://127.0.0.1:8088,http://localhost:5173}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - gem_saas_internal
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1:3001/health" ]
      interval: 30s
      timeout: 10s
      retries: 3

  # ============ POSTGRESQL DATABASE ============
  postgres:
    image: postgis/postgis:16-3.4-alpine
    container_name: gem-saas-db
    environment:
      POSTGRES_USER: ${DB_USER:-proquelec}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-securepassword}
      POSTGRES_DB: ${DB_NAME:-proquelec_saa}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/src/db/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
    ports:
      - "${GEM_DB_PORT:-5435}:5432"
    networks:
      - gem_saas_internal
    restart: unless-stopped
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER:-proquelec}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    command:
      - "postgres"
      - "-c"
      - "max_connections=250"
      - "-c"
      - "shared_buffers=256MB"
      - "-c"
      - "effective_cache_size=1GB"
      - "-c"
      - "maintenance_work_mem=64MB"
      - "-c"
      - "checkpoint_completion_target=0.9"
      - "-c"
      - "wal_buffers=16MB"
      - "-c"
      - "default_statistics_target=100"

  # ============ OSRM ROUTING ENGINE ============
  osrm:
    image: osrm/osrm-backend
    container_name: gem-saas-gps
    volumes:
      - ./backend/osrm_data:/data
    ports:
      - "${GEM_OSRM_PORT:-5010}:5000"
    command: osrm-routed --algorithm mld --max-table-size 100000 /data/senegal-latest.osrm
    restart: unless-stopped
    networks:
      - gem_saas_internal

  # ============ REDIS CACHE ============
  redis:
    image: redis:alpine
    container_name: gem-saas-cache
    restart: unless-stopped
    networks:
      - gem_saas_internal

volumes:
  postgres_data:
    driver: local
    name: gem_saas_postgres_data

networks:
  gem_saas_internal:
    driver: bridge
    name: gem_saas_internal
