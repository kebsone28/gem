{
  "name": "gem-pmtiles-server",
  "version": "0.1.0",
  "description": "Dev server for serving .pbf vector tiles (PMTiles static dev server)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch data --ext pbf,js,json --legacy-watch server.js",
    "lint": "echo \"no lint configured\" && exit 0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "keywords": ["pmtiles","tiles","vector-tiles","dev-server"],
  "author": "",
  "license": "MIT"
}
